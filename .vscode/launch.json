{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/ParseRequest/test_request",
            "args": [],
            "cwd": "${workspaceFolder}"
        }

    ]
}