{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Launch executable",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/header",   // or your built binary
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cppbuild: C/C++: g++ build active file"
    }
  ]
}